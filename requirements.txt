# ============================================================================
# LawBot - Production Requirements
#
# These are the essential packages required to run the LawBot application,
# including the Streamlit web app and the core inference pipeline.
# To install, run: pip install -r requirements.txt
# ============================================================================

# --- Core ML/AI Libraries ---
# PyTorch is the foundation for our deep learning models.
torch>=2.0.0

# Hugging Face Transformers provides models like PhoBERT and XLM-RoBERTa.
transformers>=4.30.0

# SentenceTransformers is used for creating sentence/text embeddings.
sentence-transformers>=2.2.0

# Datasets library for handling and processing data.
datasets>=2.14.0

# --- Vector Search & Indexing ---
# FAISS is used for efficient similarity search of embeddings.
# Use 'faiss-cpu' for CPU-only environments. For GPU-accelerated search,
# comment out 'faiss-cpu' and install 'faiss-gpu' manually.
faiss-cpu>=1.7.0
# faiss-gpu>=1.7.0

# --- Web Framework & UI ---
# Streamlit is used to create the interactive web application.
streamlit>=1.28.0

# --- Data Processing & Utilities ---
# Pandas and NumPy are used for data manipulation.
pandas>=1.3.0
numpy>=1.21.0

# Scikit-learn provides various utility functions.
scikit-learn>=1.0.0

# TQDM for rich progress bars in scripts.
tqdm>=4.64.0

# Back-port for type hinting on older Python versions.
typing-extensions>=4.0.0

# --- Optional for Advanced Training ---
# The following packages can be installed manually for advanced use cases
# like distributed training or experiment tracking.
# accelerate>=0.20.0
# wandb>=0.15.0
# tensorboard>=2.13.0 